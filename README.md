# RefoundImgClass
A machine learning model that automatically classifies the refund items into categories based on pictures of the items. It is running as a Fast API service in a docker image, and can be triggered by calling the Client CLI api.
