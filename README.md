# Image classification for a refund department (spotlight: Batch processing)
A machine learning model that automatically classifies refund items based on pictures of the items. It is running as a Fast API service in a docker image, and can be triggered by calling the Client CLI API.
